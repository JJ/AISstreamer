{
  "tasks": {
    "test": "deno test --allow-read --allow-write --allow-run tests/*.test.ts",
    "lint": "deno lint lib/",
    "start": "deno run --allow-env --allow-net examples/test_deno.ts"
  }
}
